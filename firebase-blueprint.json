{
  "entities": {
    "GameState": {
      "title": "GameState",
      "description": "Global game state.",
      "type": "object",
      "properties": {
        "phase": {
          "type": "string",
          "enum": ["IDLE", "PREFERENCES", "LOBBY", "QUIZ", "RESULTS"],
          "description": "The current phase of the game."
        }
      },
      "required": ["phase"]
    },
    "Player": {
      "title": "Player",
      "description": "A player in the game.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "choices": { "type": "array", "items": { "type": "string" } },
        "score": { "type": "number" },
        "timeTaken": { "type": "number" },
        "assignedRole": { "type": ["string", "null"] },
        "status": {
          "type": "string",
          "enum": ["waiting", "quiz", "finished"]
        }
      },
      "required": ["id", "name", "choices", "score", "timeTaken", "status"]
    },
    "GameConfig": {
      "title": "GameConfig",
      "description": "Game configuration like victim name and case type.",
      "type": "object",
      "properties": {
        "victimName": { "type": "string" },
        "caseType": { "type": "string" }
      },
      "required": ["victimName", "caseType"]
    }
  },
  "firestore": {
    "/game/state": {
      "schema": { "$ref": "#/entities/GameState" },
      "description": "The current game state."
    },
    "/game/config": {
      "schema": { "$ref": "#/entities/GameConfig" },
      "description": "The current game config."
    },
    "/players/{playerId}": {
      "schema": { "$ref": "#/entities/Player" },
      "description": "Players in the game."
    }
  }
}
